{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability set for the local Viable shell",
  "windows": ["main"],
  "permissions": ["core:default"]
}
